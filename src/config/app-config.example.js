// Safe defaults committed to the repo. For local secrets, use app-config.local.js
// (copy from app-config.local.example.js).

const APP_CONFIG = {
  googleDrive: {
    folderId: null,
    apiKey: null,
  },
};

export default APP_CONFIG;
